import type { Config } from 'tailwindcss';

const config: Config = {
  theme: {
    extend: {
      fontFamily: {
        inter: ['Roboto', 'sans-serif'], // Google Font
        
      },
    },
  },
  plugins: [],
};

export default config;